Hello

Please change
	ifstream myFile(file1);

	
uncomment the sorting algorithm you wish to use
    SelectionSort(arr, size);
    BubbleSort(arr, size);
    InsertionSort(arr, size);
etc

the first displayArray(arr, size); function can be commented out for faster execution